    <div class="ui inverted small menu">

      <div class="ui container">
          
      
      <a class="item">
        Chatroom
      </a>
      <a class="item">
        Messages
      </a>
      <div class="right menu">
        <div class="ui dropdown item">
          <i class="user icon"></i>
          Account   <i class="dropdown icon"></i>
          <div class="menu">
            <% if logged_in? %>
            <%= link_to "My Profile", user_path(current_user), class:"item"%>
            <%= link_to "Edit Account", edit_user_path(current_user), class: "item" %>
            <%= link_to "Logout", logout_path, method: :delete, data:{confirm: "Are you sure"}, class: "item" %>

            <% else %>
            <%= link_to "Login", login_path, class: "item" %>
            <%= link_to "Signup", new_user_path, class: "item" %>
            <% end %>
          </div>
        </div>
        
    </div>
  </div>
</div>